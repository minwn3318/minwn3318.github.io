---
title: yolo
date: 2026-04-27 10:00:00 +0900
categories: [ImageAI, framework]
tags: [ai]
---

# YOLO
## 환경준비
### 라이브러리 설치
```
!pip install ultralytics -q
```

### 라이브러리 불러오기
```
from ultralytics import settings, YOLO
import matplotlib.pyplot as plt
import cv2
import os
from IPython.display import Video
```

### YOLO 설정
- 현재 설정을 디렉토리 경로에 지정해서 놓기
```
settings
settings['datasets_dir'] = ''
settings.update()
settings
```

### 데이터 셋 다운로드
```
!pip install roboflow

from roboflow import Roboflow
rf = Roboflow(api_key="5rjzi2u0bSiMGLeqOUxf")
project = rf.workspace("roboflow-58fyf").project("rock-paper-scissors-sxsw")
version = project.version(14)
dataset = version.download("yolov11")
```

---
## 모델 사용
### 모델 다운로드
- 모델명과 탐색구분을 정의해줘야 함
```
# 이미지 경로
image_path = 'jpeg'

# 모델 선언
model = YOLO(model='yolo11n.pt', task='detect')

# confidence score와 IoU를 가지고 결과 조정 가능
high_conf = model(image_path, conf=, iou =)  
```

### 파인튜닝
- 모델명, 데이터셋, 학습수, 시드, 최적화, 등의 하이퍼파라미터를 조정할 수 있음
```
results_train = model.train(model='',
                            data='',
                            epochs=,
                            seed=,
                            optimizer = '',
                            pretrained=,
                            lr0 = 
                            )
```

### 평가결과
- 데이터 셋과 테스트 정도를 분리해야함
```
results = model.val(data=, split=)
```

### 예측
```
results = model.predict(image_path, save=True)
results[0].show()  # 탐지된 객체 출력
```