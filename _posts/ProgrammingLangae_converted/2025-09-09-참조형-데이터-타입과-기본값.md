---
title: 참조형-데이터-타입과-기본값
date: 2026-04-27 10:00:00 +0900
categories: [programming]
tags: [programming]
---

참조형 데이터 타입과 기본값
문자열 : 문자열 리터럴은 내부적으로 new string을 호출함

문자열은 불변객체
- 항상 새로운 객체를 만들어서 다시 대입함
+ 머하는 것도 새로운 객체를 만들어서 주소값을 가리키는 걸로 함

딱히 인간에게 유의미하긴 쉽지 않겠지만;;; 참조형도 비트연산이 가능한가요? 주소값을 변화줄 수 있을것 같아서 질문드립니다!
-> 가능은하지만 의미는 없다
-> 하지만 배열인덱스의 경우에는 의미가 있다, 힙메모리 안에 많이 있기 때문

그냥 문자열은 문자열 풀에 저장되어 공유
뉴 문자열은 새로운 객체를 생성하는 것

**문자열 정적 메서드 예제**

배열
- 선언시 바로 메모리에 올라가지 않음
스택에 널값이 올라감
- 생성, 할당시 크기가 얼마인지 명시해야 힙에 그 크기가 할당됨, 이때의 주소를 스택에 있는 널값에 넣어줌
-{}의미

동적배열
- 처리할 데이터의 개수가 고정된 경우가 아니라면 정적 배열은 자원을 낭비하거나 프로그램을 다시 컴파일
arrayList<> = new ArrayList();
무조건 클래스만 들어가야함

arraylist가 동적배열이 가능한 이유
자리가 다 비워지는 시점에서 새로운힙을 만들고 알아서 채워넣음

디버깅
- 브레이킹 포인트 사용
배열을 위한 반복문

메인에서 받는건 스트링 배열을 받는 아규먼트라고 명명함
외부로부터 받아서 사용할 수 있음

열거타입
- 제한된 수의 일이나 사건 등에 대하여 숫자로 표현(동적표현은 안됨)
- 제한된 사건에 대하여 숫자 대신에 상수를 정의해서 부여

enum 열거타입 {상수목록}